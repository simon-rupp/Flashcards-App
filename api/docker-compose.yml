version: '3.9'

services:
  postgres:
    image: postgres:latest
    container_name: flashcards-database
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: posts-db
    ports:
      - "5432:5432"
